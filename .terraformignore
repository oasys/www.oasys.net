# controls what directories get uploaded to TFC for remote runs
#
# deny by default
*
# explicitly list included directories
# (not recursive, needs additional entry for each nested subdir)
!terraform/
